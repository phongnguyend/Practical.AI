<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>SharePointToAzureSearch.aa86af98-b197-450b-8db6-45521bab965d</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <!-- Only the composition root. Every endpoint takes an Application contract, so the API compiles
         without a line of Entity Framework Core or a reference to the persistence layer. -->
    <ProjectReference Include="..\SharePointToAzureSearch.Infrastructure\SharePointToAzureSearch.Infrastructure.csproj" />
  </ItemGroup>

</Project>
